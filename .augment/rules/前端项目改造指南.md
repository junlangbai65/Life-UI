---
description: 当需要将前端项目改造成带开局流程、MVU 变量、统一请求处理的完整游戏界面时, 你应该参考本文件。示例实现见 备份工作区/ADven、备份工作区/Goth（原 mhjg/horr/adventure 已归档迁移）
alwaysApply: false
---

# 前端项目改造指南

## 📋 概述

本文档说明如何将一个现有的前端项目改造成带开局流程、MVU 变量、统一请求处理与编年史更新的游戏界面。

**示例实现（本仓库归档路径）：**

- `备份工作区/ADven` — 游戏式面板、消息解析等参考
- `备份工作区/Goth` — 变量读取、请求处理、编年史更新等参考

文中若仍出现历史项目名（`mhjg` / `horr` / `adventure`）或 `src/mhjg` 等路径，一律以 `备份工作区/ADven`、`备份工作区/Goth` 为准；模块文件名可能与下文不完全一致，按功能对照阅读。

## 🎯 改造目标

改造后的项目应该具备以下核心功能：

1. **开局流程管理**：根据楼层数判断是否显示开局表单
2. **变量系统集成**：使用 MVU 变量框架存储和读取游戏数据
3. **统一请求处理**：处理所有类型的游戏请求（选项、自定义等）
4. **事件监听机制**：监听消息更新事件并自动刷新界面
5. **编年史更新**：自动更新游戏编年史

## 📁 项目结构

改造后的项目应该具有以下目录结构（基于 adventure 项目）：

```
src/你的项目名/
├── index.html              # 主HTML文件
├── index.tsx 或 index.ts   # 主入口文件（React 用 .tsx，非 React 用 .ts）
├── App.tsx                 # 主应用组件（如果使用 React）
├── types.ts                # 类型定义
├── components/             # 组件目录
│   ├── GameScreen.tsx      # 游戏主界面组件（必须）
│   ├── OpeningForm.tsx     # 开局表单组件（React 项目需要）
│   ├── TitleScreen.tsx     # 标题界面组件（可选）
│   ├── ClickToStart.tsx    # 点击开始组件（可选）
│   └── ...                 # 其他组件
├── utils/                  # 工具函数目录（核心）
│   ├── gameInitializer.ts  # 游戏初始化工具（必须）
│   ├── variableReader.ts   # 变量读取工具（必须）
│   ├── requestHandler.ts   # 统一请求处理器（必须，原名 unifiedRequestHandler.ts）
│   ├── chronicleUpdater.ts # 编年史更新工具（必须）
│   ├── messageParser.ts    # 消息解析工具（推荐）
│   └── ...                 # 其他工具函数
├── services/               # 服务目录（可选）
│   └── geminiService.ts    # 如果使用 Gemini API
└── variables/              # 变量配置目录（可选）
    └── [InitVar]初始变量.json
```

**重要说明：**
- `utils/requestHandler.ts` 是统一请求处理器的文件名（在 adventure 项目中使用此命名）
- `utils/messageParser.ts` 是消息解析工具，用于从消息中提取 `<maintext>` 和 `<option>` 标签（强烈推荐）
- 如果使用 React，需要额外的组件来管理游戏阶段（TitleScreen、OpeningForm 等）

## 🔧 改造步骤（基于 adventure 项目实际实现）

> **注意**：以下步骤基于 `adventure` 项目的实际代码实现，提供了详细的代码示例和最佳实践。每个步骤都包含了完整的错误处理和日志记录。

### 步骤 0：准备工作 - 消息解析工具（推荐）

**在开始之前，建议先创建消息解析工具**，这样可以更方便地从消息中提取内容。

#### 创建 `utils/messageParser.ts`

这个工具负责从 assistant 消息中解析 `<maintext>`、`<option>`、`<mission>` 等标签：

```typescript
/**
 * 消息解析工具
 * 从最新楼层消息中解析 maintext 和 option 标签
 */

declare function getChatMessages(
  range: string | number,
  options?: { role?: 'all' | 'system' | 'assistant' | 'user' },
): Array<{ message: string; message_id: number; role: string; data?: Record<string, any> }>;
declare function getLastMessageId(): number;

/**
 * 解析消息中的正文
 * 注意：只提取不在<thinking>或<think>标签内部的<maintext>标签
 */
export function parseMaintext(messageContent: string): string {
  if (!messageContent) return '';
  
  // 先移除所有<thinking>和<think>标签及其内容
  let cleaned = messageContent.replace(/<thinking>[\s\S]*?<\/thinking>/gi, '');
  cleaned = cleaned.replace(/<think>[\s\S]*?<\/redacted_reasoning>/gi, '');
  
  // 检查是否有未闭合的标签
  const thinkingStart = cleaned.search(/<thinking>/i);
  if (thinkingStart !== -1) {
    cleaned = cleaned.substring(0, thinkingStart);
  }
  const redactedStart = cleaned.search(/<think>/i);
  if (redactedStart !== -1) {
    cleaned = cleaned.substring(0, redactedStart);
  }
  
  // 提取最后一个 <maintext> 标签
  const matches = cleaned.match(/<maintext>([\s\S]*?)<\/maintext>/gi);
  if (!matches || matches.length === 0) return '';
  const lastMatch = matches[matches.length - 1];
  const content = lastMatch.match(/<maintext>([\s\S]*?)<\/maintext>/i);
  return content ? content[1].trim() : '';
}

/**
 * 解析消息中的选项
 * 支持两种格式：
 * 1. 带 id: <option id="A">选项文本</option>
 * 2. 不带 id: <option>\nA. 选项1\nB. 选项2\n</option>
 */
export interface Option {
  id: string;
  text: string;
}

export function parseOptions(messageContent: string): Option[] {
  if (!messageContent) return [];
  
  // 先移除 thinking 和 redacted_reasoning 标签
  let cleaned = messageContent.replace(/<thinking>[\s\S]*?<\/thinking>/gi, '');
  cleaned = cleaned.replace(/<think>[\s\S]*?<\/redacted_reasoning>/gi, '');
  
  const thinkingStart = cleaned.search(/<thinking>/i);
  if (thinkingStart !== -1) {
    cleaned = cleaned.substring(0, thinkingStart);
  }
  const redactedStart = cleaned.search(/<think>/i);
  if (redactedStart !== -1) {
    cleaned = cleaned.substring(0, redactedStart);
  }
  
  // 先尝试匹配带 id 的格式
  const optionWithIdRegex = /<option id="([^"]+)">([^<]+)<\/option>/g;
  const optionsWithId: Option[] = [];
  let match;
  
  while ((match = optionWithIdRegex.exec(cleaned)) !== null) {
    optionsWithId.push({
      id: match[1],
      text: match[2].trim()
    });
  }
  
  if (optionsWithId.length > 0) {
    return optionsWithId;
  }
  
  // 尝试解析不带 id 的格式
  const optionMatch = cleaned.match(/<option>([\s\S]*?)<\/option>/i);
  if (!optionMatch) {
    return [];
  }
  
  const optionText = optionMatch[1].trim();
  const lines = optionText.split('\n').map(line => line.trim()).filter(line => line.length > 0);
  
  // 检查是否是 A.、B.、C. 格式
  const optionPattern = /^[A-Z]\.\s*/;
  const hasLetterPrefix = lines.some(line => optionPattern.test(line));
  
  if (hasLetterPrefix) {
    // 按字母开头分割选项
    const options: Option[] = [];
    let currentOption: string[] = [];
    
    for (const line of lines) {
      if (optionPattern.test(line)) {
        if (currentOption.length > 0) {
          const text = currentOption.join('\n');
          const id = text.match(/^([A-Z])\./)?.[1] || String.fromCharCode(65 + options.length);
          options.push({
            id,
            text: text.replace(/^[A-Z]\.\s*/, '').trim()
          });
          currentOption = [];
        }
        currentOption.push(line);
      } else {
        if (currentOption.length > 0) {
          currentOption.push(line);
        }
      }
    }
    
    if (currentOption.length > 0) {
      const text = currentOption.join('\n');
      const id = text.match(/^([A-Z])\./)?.[1] || String.fromCharCode(65 + options.length);
      options.push({
        id,
        text: text.replace(/^[A-Z]\.\s*/, '').trim()
      });
    }
    
    return options;
  } else {
    // 单个选项或简单的多行选项
    return lines.map((line, index) => ({
      id: String.fromCharCode(65 + index),
      text: line
    }));
  }
}

/**
 * 从最新 assistant 消息中读取正文和选项
 */
export function loadFromLatestMessage(): { 
  maintext: string; 
  options: Option[]; 
  messageId?: number; 
  userMessageId?: number;
  fullMessage?: string;
} {
  try {
    const lastMessageId = getLastMessageId();
    if (lastMessageId < 0) {
      return { maintext: '', options: [] };
    }

    // 获取最新 assistant 消息
    const messages = getChatMessages(lastMessageId, { role: 'assistant' });
    if (!messages || messages.length === 0) {
      // 尝试获取任意角色的最新消息
      const allMessages = getChatMessages(lastMessageId);
      if (!allMessages || allMessages.length === 0) {
        return { maintext: '', options: [] };
      }
      const latestMessage = allMessages[0];
      const maintext = parseMaintext(latestMessage.message || '');
      const options = parseOptions(latestMessage.message || '');
      
      // 查找对应的 user 消息
      let userMessageId: number | undefined;
      if (latestMessage.message_id > 0) {
        const userMessages = getChatMessages(latestMessage.message_id - 1, { role: 'user' });
        if (userMessages && userMessages.length > 0) {
          userMessageId = userMessages[0].message_id;
        }
      }
      
      return { 
        maintext, 
        options, 
        messageId: latestMessage.message_id,
        userMessageId,
        fullMessage: latestMessage.message
      };
    }

    const latestAssistantMessage = messages[0];
    const messageContent = latestAssistantMessage.message || '';

    const maintext = parseMaintext(messageContent);
    const options = parseOptions(messageContent);

    // 查找对应的 user 消息
    let userMessageId: number | undefined;
    if (latestAssistantMessage.message_id > 0) {
      const userMessages = getChatMessages(latestAssistantMessage.message_id - 1, { role: 'user' });
      if (userMessages && userMessages.length > 0) {
        userMessageId = userMessages[0].message_id;
      }
    }

    return { 
      maintext, 
      options, 
      messageId: latestAssistantMessage.message_id,
      userMessageId,
      fullMessage: messageContent
    };
  } catch (error) {
    console.error('❌ [messageParser] 加载最新消息失败:', error);
    return { maintext: '', options: [] };
  }
}
```

**参考实现：**
- `src/adventure/utils/messageParser.ts`

### 步骤 1：创建游戏初始化工具 (`utils/gameInitializer.ts`)

这个文件负责：
- 初始化游戏变量（写入0层消息楼层）
- 创建开局介绍楼层（1层）
- 管理世界书条目的启用/禁用（根据玩家选择）

**关键要点：**
1. **使用 `updateVariablesWith`** 确保原子性更新
2. **从0层读取并合并变量**，而不是直接覆盖
3. **创建1层消息时携带0层的 data**，确保变量正确传递
4. **防止重复创建**，检查1层消息是否已存在
5. **完整的错误处理**，包括日志记录和异常捕获

**核心功能（基于 adventure 实际实现）：**

```typescript
import { OpeningFormData } from '../types';

declare function getVariables(option: { type: 'message'; message_id: number | 'latest' }): Record<string, any>;
declare function updateVariablesWith(
  updater: (variables: Record<string, any>) => Record<string, any> | Promise<Record<string, any>>,
  option: { type: 'message'; message_id: number | 'latest' },
): Record<string, any> | Promise<Record<string, any>>;
declare function getChatMessages(range: string | number): Array<{ message: string; message_id: number }>;
declare function createChatMessages(
  messages: Array<{ role: 'user' | 'assistant' | 'system'; message: string; data?: Record<string, any> }>,
  options?: { refresh?: 'none' | 'affected' | 'all' },
): Promise<void>;
declare function getWorldbook(worldbook_name: string): Promise<WorldbookEntry[]>;
declare function replaceWorldbook(
  worldbook_name: string,
  worldbook: PartialDeep<WorldbookEntry>[],
  options?: { render?: 'debounced' | 'immediate' },
): Promise<void>;
declare const Mvu: {
  getMvuData: (options: { type: 'message' | 'chat' | 'character' | 'global'; message_id?: number | 'latest' }) => {
    stat_data: Record<string, any>;
    display_data: Record<string, any>;
    delta_data: Record<string, any>;
  };
};

// 防止重复创建的标志
let isCreatingOpening = false;

/**
 * 1. 初始化游戏变量（写入0层）
 * 注意：使用 updateVariablesWith 确保原子性更新，避免覆盖其他数据
 */
export async function initializeGameVariables(formData: OpeningFormData): Promise<boolean> {
  try {
    // 获取0层变量表（如果不存在则创建）
    let variables: any;
    try {
      variables = getVariables({ type: 'message', message_id: 0 });
    } catch (err) {
      // 如果0层消息不存在，创建一个空的变量结构
      console.warn('⚠️ 0层消息不存在，将在更新时创建', err);
      variables = { stat_data: {} };
    }

    // 使用 updateVariablesWith 更新0层变量
    await updateVariablesWith(
      vars => {
        // 确保 stat_data 存在
        if (!vars) {
          vars = { stat_data: {} };
        }
        if (!vars.stat_data) {
          vars.stat_data = {};
        }

        // 初始化游戏数据结构
        if (!vars.stat_data.游戏状态) vars.stat_data.游戏状态 = {};
        if (!vars.stat_data.公会信息) vars.stat_data.公会信息 = {};
        
        // 根据 formData 初始化变量
        vars.stat_data.游戏状态.资产 = formData.startingAssets || 20000;
        vars.stat_data.公会信息.公会名称 = formData.guildName || '你的公会名称';
        // ... 其他初始化数据

        return vars;
      },
      { type: 'message', message_id: 0 },
    );

    console.log('✅ 成功初始化0层游戏变量');
    return true;
  } catch (error) {
    console.error('❌ 初始化0层游戏变量失败:', error);
    return false;
  }
}

/**
 * 2. 创建开局介绍楼层（1层）
 * 关键要点：
 * - 防止重复创建（检查1层消息是否已存在）
 * - 获取0层的 data 并携带到1层
 * - 根据配置生成不同风格的开局文本
 * - 管理世界书条目（根据叙事风格启用/禁用）
 * - 创建完成后更新编年史
 */
export async function createOpeningStoryMessage(formData: OpeningFormData): Promise<boolean> {
  // 防止重复创建
  if (isCreatingOpening) {
    console.log('⚠️ 正在创建开局楼层，跳过重复调用');
    return false;
  }

  try {
    // 检查是否已经存在1层消息，避免重复创建
    try {
      const existingMessages = getChatMessages(1);
      if (existingMessages && existingMessages.length > 0) {
        console.log('⚠️ 1层消息已存在，跳过创建');
        // 即使已存在，也尝试更新编年史（以防之前更新失败）
        setTimeout(async () => {
          try {
            const { checkAndUpdateChronicle } = await import('./chronicleUpdater');
            await new Promise(resolve => setTimeout(resolve, 500));
            await checkAndUpdateChronicle();
          } catch (error) {
            console.error('❌ 更新编年史失败:', error);
          }
        }, 500);
        return true;
      }
    } catch (err) {
      // 1层不存在，继续创建
    }

    // 设置创建标志
    isCreatingOpening = true;

    // 根据 formData 构建开局文本（这里简化示例，实际可以更复杂）
    const maintext = `<maintext>
欢迎来到${formData.guildName || '你的公会'}！

这是一个全新的开始...
</maintext>`;

    // 构建 option 内容
    const option = `<option>
开始这个故事
</option>`;

    // 添加 sum 标签（编年史摘要）
    const sum = `<sum>你创建了公会"${formData.guildName || '你的公会'}"，初始资产 ${formData.startingAssets || 20000}。一切准备就绪，故事即将开始。</sum>`;

    // 组合完整消息
    const message = `${maintext}\n\n${sum}\n\n${option}`;

    // 获取0层的data（携带变量）
    let layer0Data: any = { stat_data: {}, display_data: {}, delta_data: {} };
    try {
      const mvuData = Mvu.getMvuData({ type: 'message', message_id: 0 });
      // 确保返回的数据结构完整
      if (mvuData && mvuData.stat_data) {
        layer0Data = mvuData;
      } else {
        console.warn('⚠️ 0层MVU数据的 stat_data 不存在，使用空对象');
      }
    } catch (err) {
      console.warn('⚠️ 获取0层MVU数据失败，尝试从getVariables读取', err);
      try {
        const vars = getVariables({ type: 'message', message_id: 0 });
        if (vars && vars.stat_data) {
          layer0Data = {
            stat_data: vars.stat_data || {},
            display_data: vars.display_data || {},
            delta_data: vars.delta_data || {},
          };
        } else {
          console.warn('⚠️ 0层变量的 stat_data 不存在，使用空对象');
        }
      } catch (err2) {
        console.warn('⚠️ 获取0层变量失败，使用空对象', err2);
      }
    }

    // 创建1层assistant消息，携带0层的data
    await createChatMessages(
      [
        {
          role: 'assistant',
          message: message,
          data: layer0Data, // 携带0层的data，确保变量正确传递
        },
      ],
      {
        refresh: 'none', // 不刷新显示，由游戏界面自己读取
      },
    );

    console.log('✅ 成功创建开局介绍楼层（1层）');

    // 根据配置管理世界书条目（可选）
    // 例如：根据叙事风格启用/禁用对应的世界书条目
    try {
      // 这里可以根据 formData.narrativeStyle 来启用/禁用对应的世界书条目
      // 示例代码请参考 adventure 项目的实际实现
    } catch (error) {
      console.error('❌ 更新世界书条目失败:', error);
      // 不抛出错误，继续执行后续流程
    }

    // 创建完成后，延迟更新编年史（等待消息写入完成）
    const updateChronicleWithRetry = async (retries = 3) => {
      for (let i = 0; i < retries; i++) {
        try {
          await new Promise(resolve => setTimeout(resolve, 500 * (i + 1))); // 递增延迟
          const { checkAndUpdateChronicle } = await import('./chronicleUpdater');
          await checkAndUpdateChronicle();
          console.log('✅ 编年史更新成功');
          return;
        } catch (error) {
          console.warn(`⚠️ 编年史更新失败（尝试 ${i + 1}/${retries}）:`, error);
          if (i === retries - 1) {
            console.error('❌ 编年史更新最终失败:', error);
          }
        }
      }
    };
    updateChronicleWithRetry();

    // 重置创建标志
    isCreatingOpening = false;
    return true;
  } catch (error) {
    console.error('❌ 创建开局介绍楼层失败:', error);
    // 重置创建标志
    isCreatingOpening = false;
    return false;
  }
}
```

**关键实践要点：**
1. ✅ **防止重复创建**：使用全局标志 `isCreatingOpening` 和检查1层消息是否存在
2. ✅ **完整的错误处理**：每个步骤都有 try-catch，关键步骤有重试机制
3. ✅ **变量传递**：确保0层的 data 正确传递到1层
4. ✅ **世界书管理**：根据配置自动启用/禁用相关条目（可选功能）
5. ✅ **编年史更新**：使用重试机制确保更新成功

**参考实现：**
- `src/horr/utils/gameInitializer.ts`
- `src/mhjg/utils/gameInitializer.ts`

### 步骤 2：创建变量读取工具 (`utils/variableReader.ts`)

这个文件负责：
- 从最新消息楼层读取游戏变量
- 处理 MVU 格式的数据（支持 `[值, "描述"]` 格式）
- 提供类型安全的数据读取接口
- 支持多层级回退（从最新 assistant → 最新楼层 → 0层）

**关键要点（基于 adventure 实际实现）：**
1. **优先从最新 assistant 消息读取**：因为变量更新通常写在最新的 assistant 消息中
2. **支持多种读取方式**：MVU.getMvuData、消息的 data 字段、getVariables
3. **处理 MVU 格式**：使用 `pick` 函数支持 `[值, "描述"]` 格式
4. **完整的错误处理和日志**：每个步骤都有详细的日志记录

**核心功能（基于 adventure 实际实现）：**

```typescript
import { YourGameData } from '../types';

declare function getVariables(option: { type: 'message'; message_id: number | 'latest' }): Record<string, any>;
declare function waitGlobalInitialized<T>(global: 'Mvu' | string): Promise<T>;
declare function getChatMessages(
  range: string | number, 
  options?: { role?: 'user' | 'assistant' | 'system' }
): Array<{ message_id: number; role: string; data?: Record<string, any> }>;
declare function getLastMessageId(): number;

declare const Mvu: {
  getMvuData: (options: { type: 'message' | 'chat' | 'character' | 'global'; message_id?: number | 'latest' }) => {
    stat_data: Record<string, any>;
    display_data: Record<string, any>;
    delta_data: Record<string, any>;
  };
};

// MVU 初始化状态
let mvuInitialized: boolean = false;
let mvuInitPromise: Promise<void> | null = null;

type Value = string | number | boolean | Record<string, any> | Array<any> | null | undefined;

/**
 * 从嵌套对象中提取值，支持 MVU 格式 [值, "描述"]
 */
function pick<T extends Value>(obj: any, path: string, fallback: T): T {
  if (!obj) return fallback;
  const parts = path.split('.');
  let cur: any = obj;
  for (const p of parts) {
    if (cur == null) return fallback;
    // 处理 MVU 格式 [值, "描述"]
    if (Array.isArray(cur) && cur.length > 0) {
      cur = cur[0];
    }
    cur = cur[p];
  }
  // 如果最终值是 MVU 格式，返回第一个元素（实际值）
  if (Array.isArray(cur) && cur.length > 0) return (cur[0] as T) ?? fallback;
  return (cur as T) ?? fallback;
}

/**
 * 确保 MVU 已初始化
 */
async function ensureMvuInitialized(): Promise<void> {
  if (mvuInitialized) {
    return;
  }

  if (mvuInitPromise) {
    return mvuInitPromise;
  }

  mvuInitPromise = (async () => {
    try {
      await waitGlobalInitialized('Mvu');
      mvuInitialized = true;
      console.log('✅ MVU 初始化完成');
    } catch (error) {
      console.warn('⚠️ 等待 MVU 初始化失败:', error);
      mvuInitialized = true;
    }
  })();

  return mvuInitPromise;
}

/**
 * 检查 stat_data 是否有实际内容（不是空对象）
 */
function hasStatDataContent(stat_data: any): boolean {
  if (!stat_data || typeof stat_data !== 'object') {
    return false;
  }
  return Object.keys(stat_data).length > 0;
}

/**
 * 从最新消息楼层读取 MVU 数据
 * 读取优先级：
 * 1. 最新 assistant 消息的 MVU 数据（通过 replaceMvuData 写入）
 * 2. 最新 assistant 消息的 data 字段
 * 3. 最新楼层的 MVU 数据
 * 4. 最新楼层的变量数据（通过 getVariables）
 * 5. 0层的 MVU 数据（作为初始化数据）
 */
async function getGameMvuData(): Promise<{ stat_data: Record<string, any>; display_data?: Record<string, any> }> {
  // 确保 MVU 已初始化
  await ensureMvuInitialized();

  // 优先从最新的 assistant 消息读取
  try {
    const assistantMessages = getChatMessages(-1, { role: 'assistant' });
    if (assistantMessages && assistantMessages.length > 0) {
      const latestAssistant = assistantMessages[assistantMessages.length - 1];
      const messageId = latestAssistant.message_id;

      console.log(`🔍 [variableReader] 尝试从最新 assistant 消息（ID: ${messageId}）读取变量数据`);

      // 优先尝试从该 assistant 消息读取 MVU 数据（通过 replaceMvuData 写入的数据）
      try {
        const mvuData = Mvu.getMvuData({ type: 'message', message_id: messageId });
        if (mvuData && mvuData.stat_data && hasStatDataContent(mvuData.stat_data)) {
          console.log(`✅ [variableReader] 从最新 assistant 消息（ID: ${messageId}）读取 MVU 数据成功`);
          return mvuData;
        } else {
          console.log(`⚠️ [variableReader] 从消息 ${messageId} 读取的 MVU 数据为空或无效`);
        }
      } catch (err) {
        console.warn(`⚠️ [variableReader] 从 assistant 消息 ${messageId} 读取 MVU 数据失败:`, err);
      }

      // 尝试从该 assistant 消息的 data 字段读取（备用方案）
      if (latestAssistant.data && latestAssistant.data.stat_data && hasStatDataContent(latestAssistant.data.stat_data)) {
        console.log(`✅ [variableReader] 从最新 assistant 消息（ID: ${messageId}）的 data 字段读取变量数据`);
        return {
          stat_data: latestAssistant.data.stat_data || {},
          display_data: latestAssistant.data?.display_data,
        };
      } else {
        console.log(`⚠️ [variableReader] 消息 ${messageId} 的 data 字段为空或无效`);
      }
    }
  } catch (err) {
    console.warn('⚠️ [variableReader] 获取最新 assistant 消息失败，尝试其他方式', err);
  }

  // 退化：使用 Mvu.getMvuData 读取最新楼层变量
  try {
    const mvuData = Mvu.getMvuData({ type: 'message', message_id: 'latest' });
    if (mvuData && mvuData.stat_data && hasStatDataContent(mvuData.stat_data)) {
      console.log('✅ 从最新楼层读取 MVU 数据');
      return mvuData;
    }
  } catch (err) {
    console.warn('⚠️ Mvu.getMvuData(latest) 失败，尝试从 getVariables 读取', err);
  }

  // 退化：使用 getVariables 读取最新楼层变量
  try {
    const variables = getVariables({ type: 'message', message_id: 'latest' });
    if (variables && variables.stat_data && hasStatDataContent(variables.stat_data)) {
      console.log('✅ 从最新楼层读取变量数据（通过 getVariables）');
      return {
        stat_data: variables.stat_data || {},
        display_data: variables?.display_data,
      };
    }
  } catch (err) {
    console.warn('⚠️ 无法获取最新楼层变量，尝试读取0层', err);
  }

  // 如果最新楼层没有数据，尝试读取0层（用于初始化数据）
  try {
    const mvuData = Mvu.getMvuData({ type: 'message', message_id: 0 });
    if (mvuData && mvuData.stat_data && hasStatDataContent(mvuData.stat_data)) {
      console.log('✅ 从0层读取 MVU 数据（最新楼层无数据）');
      return mvuData;
    }
  } catch (err) {
    console.warn('⚠️ Mvu.getMvuData(0) 失败', err);
  }

  try {
    const variables = getVariables({ type: 'message', message_id: 0 });
    if (variables && variables.stat_data && hasStatDataContent(variables.stat_data)) {
      console.log('✅ 从0层读取变量数据（通过 getVariables）');
      return {
        stat_data: variables.stat_data || {},
        display_data: variables?.display_data,
      };
    }
  } catch (err) {
    console.warn('⚠️ 无法获取0层变量，返回空对象', err);
  }

  console.warn('⚠️ 无法获取任何楼层的数据，返回空对象');
  return { stat_data: {} };
}

/**
 * 从最新消息楼层读取游戏数据（用于界面展示）
 * 使用 pick 函数支持 MVU 格式 [值, "描述"]
 */
export async function readYourGameData(): Promise<YourGameData> {
  const m = await getGameMvuData();
  const stat = m?.stat_data || {};

  console.log('🔍 [variableReader] stat_data 内容:', stat);

  // 使用 pick 函数从嵌套对象中提取值，支持 MVU 格式
  const gameStatusRaw = pick(stat, '游戏状态', {});
  let gameStatus: any = gameStatusRaw;
  if (Array.isArray(gameStatusRaw) && gameStatusRaw.length > 0) {
    gameStatus = gameStatusRaw[0];
  }

  // 读取其他数据...
  const 你的数据 = pick(stat, '你的数据路径', 默认值);
  const 另一个数据 = pick(stat, '另一个数据路径', 另一个默认值);

  console.log('✅ [variableReader] 解析结果:', {
    // 日志摘要
  });

  return {
    你的数据,
    另一个数据,
    // ... 其他数据
  };
}
```

**参考实现：**
- `src/horr/utils/variableReader.ts`
- `src/mhjg/utils/variableReader.ts`

### 步骤 3：创建统一请求处理器 (`utils/unifiedRequestHandler.ts`)

这个文件负责：
- 处理所有类型的游戏请求（选项、自定义等）
- 支持流式生成
- 移除 `<thinking>` 标签
- 提取消息中的标签（`<maintext>`、`<option>`、`<sum>`、`<UpdateVariable>` 等）
- 使用 MVU 解析消息中的变量更新命令
- 创建 assistant 消息并携带变量数据

**核心功能：**

```typescript
export type RequestType = 'option' | 'custom' | 'your-custom-type';

export interface RequestData {
  type: RequestType;
  content: string;
}

export async function handleUnifiedRequest(
  request: RequestData,
  callbacks: {
    onDisableOptions?: () => void;
    onShowGenerating?: () => void;
    onHideGenerating?: () => void;
    onEnableOptions?: () => void;
    onError?: (error: string) => void;
    onRefreshStory?: () => void;
    onStreamingUpdate?: (text: string) => void;
    onRefreshVariables?: () => void;
  },
): Promise<boolean> {
  let userMessageId: number | null = null;

  try {
    // 1. 禁用所有选项按钮
    if (callbacks.onDisableOptions) {
      callbacks.onDisableOptions();
    }

    // 2. 显示"剧情正在生成"提示框
    if (callbacks.onShowGenerating) {
      callbacks.onShowGenerating();
    }

    // 3. 构建提示词
    const prompt = buildRequestPrompt(request);

    // 4. 获取基础 MVU 数据（用于传递到 user 消息）
    const mvu_data = await getBaseMvuData();

    // 5. 创建 user 消息，传递 MVU 数据
    await createChatMessages(
      [
        {
          role: 'user',
          message: prompt,
          data: mvu_data,
        },
      ],
      {
        refresh: 'none',
      },
    );

    // 6. 获取刚创建的 user 消息 ID
    userMessageId = getLastMessageId();

    // 7. 注册流式事件监听器（在调用 generate 之前）
    let streamingHandler: ((text: string) => void) | null = null;
    if (typeof eventOn !== 'undefined' && iframe_events?.STREAM_TOKEN_RECEIVED_FULLY) {
      streamingHandler = (fullText: string) => {
        const cleanedText = removeThinkingTagsFromStream(fullText);
        if (cleanedText && cleanedText.trim().length > 0) {
          const maintextMatch = cleanedText.match(/<maintext>([\s\S]*?)(?:<\/maintext>|$)/i);
          if (maintextMatch && maintextMatch[1]) {
            if (callbacks.onStreamingUpdate) {
              callbacks.onStreamingUpdate(maintextMatch[1].trim());
            }
          }
        }
      };
      eventOn(iframe_events.STREAM_TOKEN_RECEIVED_FULLY, streamingHandler);
    }

    // 8. 使用 generate 生成 LLM 回复（支持流式生成）
    const result = await generate({
      user_input: prompt,
      should_stream: true,
    });

    // 9. 移除 <thinking> 标签
    const cleanedResult = removeThinkingTags(result);

    // 10. 验证返回结果是否符合规范
    if (!cleanedResult || !validateMessage(cleanedResult)) {
      // 删除 user 消息并返回错误
      if (userMessageId !== null) {
        await deleteChatMessages([userMessageId], { refresh: 'none' });
      }
      if (callbacks.onHideGenerating) callbacks.onHideGenerating();
      if (callbacks.onEnableOptions) callbacks.onEnableOptions();
      if (callbacks.onError) callbacks.onError('生成的消息不符合规范');
      return false;
    }

    // 11. 提取最后一次出现的标签
    const maintext = extractLastMaintext(cleanedResult);
    const option = extractLastOption(cleanedResult);
    const sum = extractLastSum(cleanedResult);
    const updateVariable = extractLastUpdateVariable(cleanedResult);

    // 12. 重新组合消息
    let finalMessage = `<maintext>${maintext}</maintext>\n\n<option>${option}</option>`;
    if (sum) finalMessage += `\n\n<sum>${sum}</sum>`;
    if (updateVariable) finalMessage += `\n\n<UpdateVariable>${updateVariable}</UpdateVariable>`;

    // 13. 确保 MVU 已初始化
    await waitGlobalInitialized('Mvu');

    // 14. 获取基础 MVU 数据
    let base = await getBaseMvuData();

    // 15. 解析消息中的 MVU 命令
    let finalData = base;
    if (typeof Mvu !== 'undefined') {
      try {
        const parsed = await Mvu.parseMessage(finalMessage, base);
        if (parsed) {
          finalData = parsed;
        }
      } catch (err) {
        console.warn('⚠️ 解析 MVU 命令失败', err);
      }
    }

    // 16. 创建 assistant 消息，传入解析后的数据
    await createChatMessages(
      [
        {
          role: 'assistant',
          message: finalMessage,
          data: finalData,
        },
      ],
      {
        refresh: 'none',
      },
    );

    // 17. 更新编年史
    const { checkAndUpdateChronicle } = await import('./chronicleUpdater');
    await checkAndUpdateChronicle();

    // 18. 隐藏生成提示框
    if (callbacks.onHideGenerating) {
      callbacks.onHideGenerating();
    }

    // 19. 刷新游戏显示
    setTimeout(() => {
      if (callbacks.onRefreshStory) {
        callbacks.onRefreshStory();
      }
    }, 300);

    // 20. 重新启用选项按钮
    if (callbacks.onEnableOptions) {
      callbacks.onEnableOptions();
    }

    return true;
  } catch (error) {
    // 错误处理...
    return false;
  }
}
```

**参考实现：**
- `src/horr/utils/unifiedRequestHandler.ts`
- `src/mhjg/utils/unifiedRequestHandler.ts`

### 步骤 4：创建编年史更新工具 (`utils/chronicleUpdater.ts`)

这个文件负责：
- 从最新消息中提取 `<sum>` 标签内容
- 更新世界书中的编年史条目

**核心功能：**

```typescript
export async function checkAndUpdateChronicle(): Promise<void> {
  try {
    // 获取最新消息
    const messages = getChatMessages(-1);
    if (!messages || messages.length === 0) return;

    const latestMessage = messages[messages.length - 1];
    const messageContent = latestMessage.message || '';

    // 提取 <sum> 标签内容
    const sumMatch = messageContent.match(/<sum>([\s\S]*?)<\/sum>/i);
    if (!sumMatch || !sumMatch[1]) return;

    const sumContent = sumMatch[1].trim();
    const messageId = latestMessage.message_id;

    // 更新世界书中的编年史条目
    const worldbookName = '你的世界书名';
    const worldbookEntries = await getWorldbook(worldbookName);
    
    // 查找编年史条目并更新
    // ...

    await replaceWorldbook(worldbookName, updatedEntries);
  } catch (error) {
    console.error('❌ 更新编年史失败:', error);
  }
}
```

**参考实现：**
- `src/horr/utils/chronicleUpdater.ts`
- `src/mhjg/utils/chronicleUpdater.ts`

### 步骤 5：修改主入口文件 (`index.tsx` 或 `index.ts`)

在主入口文件中实现开局流程判断：

**React 项目（index.tsx）：**

```typescript
import React, { useState, useEffect } from 'react';
import { createRoot } from 'react-dom/client';
import App from './App';

$(() => {
  const container = document.getElementById('app');
  if (!container) return;

  const root = createRoot(container);
  root.render(React.createElement(App));
});
```

**非 React 项目（index.ts）：**

```typescript
$(() => {
  // 检查当前楼层数
  try {
    const lastMessageId = getLastMessageId();
    console.log('📊 当前楼层数:', lastMessageId);

    if (lastMessageId === 0) {
      // 楼层为0，显示开局表单
      initializeOpeningForm();
    } else {
      // 楼层不为0，直接进入游戏页面
      initializeGame();
    }
  } catch (error) {
    console.error('❌ 检测楼层数失败，默认显示开局表单:', error);
    initializeOpeningForm();
  }
});
```

### 步骤 6：修改主应用组件 (`App.tsx` 或主组件)

在主应用组件中实现游戏阶段管理：

**React 项目示例：**

```typescript
import React, { useState, useEffect } from 'react';
import { GameScreen } from './components/GameScreen';
import { OpeningForm } from './components/OpeningForm'; // 你的开局表单组件
import { initializeGameVariables, createOpeningStoryMessage } from './utils/gameInitializer';
import { FormData } from './types';

// 游戏阶段枚举
enum GamePhase {
  TITLE = 'title',
  OPENING = 'opening',
  GAME = 'game',
}

const App: React.FC = () => {
  const [phase, setPhase] = useState<GamePhase>(GamePhase.TITLE);
  const [formData, setFormData] = useState<FormData | null>(null);

  // 检查楼层数，决定初始阶段
  useEffect(() => {
    const checkMessageId = () => {
      try {
        const lastMessageId = getLastMessageId();
        console.log('📊 当前楼层数:', lastMessageId);

        if (lastMessageId === 0) {
          // 楼层为0，显示开局表单
          setPhase(GamePhase.OPENING);
        } else {
          // 楼层不为0，直接进入游戏页面
          setPhase(GamePhase.GAME);
        }
      } catch (error) {
        console.error('❌ 检测楼层数失败，默认显示开局表单:', error);
        setPhase(GamePhase.OPENING);
      }
    };

    checkMessageId();
  }, []);

  // 处理开局表单提交
  const handleOpeningSubmit = async (formData: FormData) => {
    setFormData(formData);

    // 初始化游戏变量（写入0层）
    console.log('🎮 开始初始化游戏变量...');
    const initSuccess = await initializeGameVariables(formData);
    if (!initSuccess) {
      console.error('❌ 初始化游戏变量失败，但继续进入游戏');
    }

    // 创建开局介绍楼层（1层）
    console.log('📖 开始创建开局介绍楼层...');
    const storySuccess = await createOpeningStoryMessage(formData);
    if (!storySuccess) {
      console.error('❌ 创建开局介绍楼层失败，但继续进入游戏');
    }

    // 进入游戏阶段
    setPhase(GamePhase.GAME);
  };

  return (
    <div className="app">
      {phase === GamePhase.OPENING && (
        <OpeningForm onSubmit={handleOpeningSubmit} />
      )}
      {phase === GamePhase.GAME && <GameScreen />}
    </div>
  );
};

export default App;
```

**非 React 项目示例：**

```typescript
// 游戏阶段
type GamePhase = 'opening' | 'game';
let currentPhase: GamePhase = 'opening';

// 初始化开局表单
function initializeOpeningForm(): void {
  const app = document.getElementById('app');
  if (!app) return;

  app.innerHTML = `
    <div class="opening-form">
      <!-- 你的开局表单HTML -->
    </div>
  `;

  // 绑定表单提交事件
  const form = app.querySelector('form');
  if (form) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      const formData = extractFormData(form);
      
      // 初始化游戏变量
      await initializeGameVariables(formData);
      
      // 创建开局介绍楼层
      await createOpeningStoryMessage(formData);
      
      // 进入游戏阶段
      currentPhase = 'game';
      initializeGame();
    });
  }
}

// 初始化游戏主界面
function initializeGame(): void {
  const app = document.getElementById('app');
  if (!app) return;

  app.innerHTML = `
    <div class="game-screen">
      <!-- 你的游戏界面HTML -->
    </div>
  `;

  // 初始化游戏界面组件
  // ...
}
```

### 步骤 7：实现事件监听机制

在游戏主界面中监听消息更新事件，自动刷新界面数据：

**React 项目示例（在 GameScreen 组件中）：**

```typescript
import React, { useState, useEffect, useCallback } from 'react';
import { readYourGameData } from '../utils/variableReader';
import { YourGameData } from '../types';

export const GameScreen: React.FC = () => {
  const [gameData, setGameData] = useState<YourGameData | null>(null);
  const [isRefreshing, setIsRefreshing] = useState(false);

  // 刷新游戏数据
  const refreshGameData = useCallback(async () => {
    if (isRefreshing) {
      console.log('⚠️ 正在刷新中，跳过重复刷新');
      return;
    }

    setIsRefreshing(true);
    try {
      const data = await readYourGameData();
      setGameData(data);
      console.log('✅ 已刷新游戏数据');
    } catch (error) {
      console.error('❌ 刷新游戏数据失败:', error);
    } finally {
      setIsRefreshing(false);
    }
  }, [isRefreshing]);

  // 初始化时加载数据
  useEffect(() => {
    refreshGameData();
  }, []);

  // 监听消息更新事件
  useEffect(() => {
    let pendingRefreshTimer: number | null = null;

    // 监听消息更新事件（消息被修改时，包括 replaceMvuData 更新变量）
    eventOn(tavern_events.MESSAGE_UPDATED, (message_id: number) => {
      console.log('🔄 消息已更新，刷新游戏数据:', message_id);

      // 清除之前的延迟刷新定时器
      if (pendingRefreshTimer !== null) {
        clearTimeout(pendingRefreshTimer);
        pendingRefreshTimer = null;
      }

      // 延迟刷新，确保 replaceMvuData 完全完成且数据已写入
      pendingRefreshTimer = window.setTimeout(async () => {
        await refreshGameData();
      }, 300);
    });

    // 监听消息接收事件（LLM返回新消息时）
    eventOn(tavern_events.MESSAGE_RECEIVED, async (message_id: number) => {
      console.log('📨 收到新消息:', message_id);
      // 延迟刷新，等待 MESSAGE_UPDATED 事件
      setTimeout(() => {
        if (!isRefreshing) {
          refreshGameData();
        }
      }, 1000);
    });

    // 清理函数
    return () => {
      if (pendingRefreshTimer !== null) {
        clearTimeout(pendingRefreshTimer);
      }
    };
  }, [refreshGameData, isRefreshing]);

  if (!gameData) {
    return <div>加载中...</div>;
  }

  return (
    <div className="game-screen">
      {/* 你的游戏界面 */}
    </div>
  );
};
```

**非 React 项目示例：**

```typescript
// 刷新所有组件的数据（从变量表重新读取）
function refreshAllComponents(): void {
  // 刷新各个组件
  if (yourComponent) {
    yourComponent.loadFromVariables();
  }
  // ... 刷新其他组件
}

// 初始化事件监听
function initializeEventListeners(): void {
  let isRefreshing = false;
  let pendingRefreshTimer: number | null = null;

  // 监听消息更新事件
  eventOn(tavern_events.MESSAGE_UPDATED, (message_id: number) => {
    console.log('🔄 消息已更新，刷新游戏数据:', message_id);

    if (pendingRefreshTimer !== null) {
      clearTimeout(pendingRefreshTimer);
      pendingRefreshTimer = null;
    }

    if (isRefreshing) {
      console.log('⚠️ 正在刷新中，跳过重复刷新');
      return;
    }

    pendingRefreshTimer = window.setTimeout(async () => {
      isRefreshing = true;
      try {
        // 先检查并更新编年史
        const { checkAndUpdateChronicle } = await import('./utils/chronicleUpdater');
        await checkAndUpdateChronicle();

        // 刷新所有组件
        refreshAllComponents();
        console.log('✅ 已刷新所有组件（MESSAGE_UPDATED）');
      } finally {
        isRefreshing = false;
        pendingRefreshTimer = null;
      }
    }, 300);
  });

  // 监听消息接收事件
  eventOn(tavern_events.MESSAGE_RECEIVED, async (message_id: number) => {
    console.log('📨 收到新消息:', message_id);
    setTimeout(() => {
      if (!isRefreshing) {
        refreshAllComponents();
      }
    }, 1000);
  });
}
```

### 步骤 8：在 GameScreen 中集成统一请求处理器

在游戏主界面中使用统一请求处理器处理用户操作：

**React 项目示例：**

```typescript
import React, { useState, useCallback } from 'react';
import { handleUnifiedRequest, RequestData } from '../utils/unifiedRequestHandler';
import { readYourGameData } from '../utils/variableReader';

export const GameScreen: React.FC = () => {
  const [isGenerating, setIsGenerating] = useState(false);
  const [streamingText, setStreamingText] = useState('');
  const [optionsDisabled, setOptionsDisabled] = useState(false);

  // 处理选项点击
  const handleOptionClick = useCallback(async (optionText: string) => {
    const request: RequestData = {
      type: 'option',
      content: optionText,
    };

    const success = await handleUnifiedRequest(request, {
      onDisableOptions: () => setOptionsDisabled(true),
      onShowGenerating: () => setIsGenerating(true),
      onHideGenerating: () => setIsGenerating(false),
      onEnableOptions: () => setOptionsDisabled(false),
      onError: (error: string) => {
        console.error('❌ 请求处理失败:', error);
        alert(error);
      },
      onRefreshStory: async () => {
        // 刷新游戏数据
        const data = await readYourGameData();
        // 更新界面...
      },
      onStreamingUpdate: (text: string) => {
        setStreamingText(text);
      },
    });

    if (success) {
      setStreamingText(''); // 清空流式文本
    }
  }, []);

  return (
    <div className="game-screen">
      {/* 主文本显示区域 */}
      <div className="main-text">
        {streamingText || gameData?.maintext || '加载中...'}
      </div>

      {/* 选项按钮 */}
      <div className="options">
        {gameData?.options?.map((option, index) => (
          <button
            key={index}
            onClick={() => handleOptionClick(option)}
            disabled={optionsDisabled || isGenerating}
          >
            {option}
          </button>
        ))}
      </div>

      {/* 生成提示框 */}
      {isGenerating && (
        <div className="generating-overlay">
          剧情正在生成...
        </div>
      )}
    </div>
  );
};
```

**非 React 项目示例：**

```typescript
// 处理选项点击
async function handleOptionClick(optionText: string): Promise<void> {
  const request: RequestData = {
    type: 'option',
    content: optionText,
  };

  await handleUnifiedRequest(request, {
    onDisableOptions: () => {
      // 禁用所有选项按钮
      document.querySelectorAll('.option-button').forEach(btn => {
        (btn as HTMLButtonElement).disabled = true;
      });
    },
    onShowGenerating: () => {
      // 显示生成提示框
      const overlay = document.getElementById('generating-overlay');
      if (overlay) overlay.style.display = 'block';
    },
    onHideGenerating: () => {
      // 隐藏生成提示框
      const overlay = document.getElementById('generating-overlay');
      if (overlay) overlay.style.display = 'none';
    },
    onEnableOptions: () => {
      // 启用所有选项按钮
      document.querySelectorAll('.option-button').forEach(btn => {
        (btn as HTMLButtonElement).disabled = false;
      });
    },
    onError: (error: string) => {
      console.error('❌ 请求处理失败:', error);
      alert(error);
    },
    onRefreshStory: async () => {
      // 刷新游戏数据
      await refreshAllComponents();
    },
    onStreamingUpdate: (text: string) => {
      // 更新流式文本显示
      const mainTextElement = document.getElementById('main-text');
      if (mainTextElement) {
        mainTextElement.textContent = text;
      }
    },
  });
}
```

### 步骤 9：实现样式规范（基于 adventure 项目）

样式规范是项目的重要组成部分，确保界面的一致性和美观性。本节基于 `adventure` 项目的实际实现，提供完整的样式规范指南。

#### 9.1 CSS 变量系统（语义化变量）

**核心原则：使用语义化变量名，而非具体的颜色值**

创建 `components/GameScreen.css` 或 `styles/variables.scss`（基于 adventure 实现）：

```css
/* ============================================
   CSS 变量系统 - 语义化变量
   ============================================ */
:root {
  /* ============================================
     语义化变量系统 (Semantic Variable System)
     ============================================ */

  /* 默认主题 (Alchemy/Gold) - 深色主题 */
  --theme-primary: hsl(45, 70%, 55%);
  --theme-primary-dim: hsl(45, 50%, 40%);
  --theme-primary-glow: hsl(45, 80%, 70%);
  --theme-secondary: hsl(45, 20%, 70%);

  /* 背景色系统 */
  --theme-bg-main: hsl(210, 15%, 8%);
  --theme-bg-panel: hsl(210, 15%, 12%);
  --theme-bg-card: hsl(210, 15%, 14%);
  --theme-bg-overlay: rgba(0, 0, 0, 0.85);
  --theme-bg-glass: hsla(210, 15%, 10%, 0.85);

  /* 边框系统 */
  --theme-border: hsla(45, 70%, 55%, 0.2);
  --theme-border-hover: hsla(45, 70%, 55%, 0.5);

  /* 文字颜色系统 */
  --theme-text-main: hsl(45, 30%, 90%);
  --theme-text-secondary: hsl(45, 20%, 70%);
  --theme-text-dim: hsl(45, 15%, 50%);

  /* 阴影系统 */
  --theme-shadow: hsla(45, 70%, 55%, 0.3);
  --theme-shadow-strong: hsla(45, 70%, 55%, 0.5);
  --theme-shadow-flat: 0 4px 12px var(--theme-shadow);
  --theme-shadow-raised: 0 8px 24px var(--theme-shadow-strong);

  /* 动画时长 */
  --transition-fast: 0.2s;
  --transition-normal: 0.3s;
  --transition-slow: 0.5s;
  --transition-slower: 0.8s;

  /* 富文本配色 */
  --rich-text-highlight: var(--theme-primary);
  --rich-text-emphasis: hsl(25, 100%, 65%);
  --rich-text-special: hsl(258, 85%, 75%);
  --rich-text-quote: hsl(180, 70%, 70%);

  /* 特殊颜色 */
  --text-hextech: hsl(175, 70%, 55%);
  --text-alert: hsl(0, 60%, 55%);
}
```

**关键要点：**
- ✅ 使用语义化变量名（`--theme-primary` 而非 `--gold-color`）
- ✅ 使用 HSL 颜色格式，便于调整明度和饱和度
- ✅ 提供多层次的背景色（main、panel、card、overlay、glass）
- ✅ 提供多层次的文字颜色（main、secondary、dim）
- ✅ 统一的过渡动画时长变量

#### 9.2 主题系统（多主题支持）

**实现多主题切换（基于 adventure 实际实现）：**

```css
/* ============================================
   Relaxed 主题 (Light/Pastel)
   ============================================ */
[data-theme="relaxed"] {
  /* 柔和的青色/鼠尾草绿作为主色调 */
  --theme-primary: #52be80;
  --theme-primary-dim: hsl(160, 25%, 35%);
  --theme-primary-glow: rgba(82, 190, 128, 0.4);
  --theme-secondary: hsl(30, 20%, 60%);
  --theme-accent: #f39c12;

  /* 奶油白/浅灰背景 */
  --theme-bg-main: #f4f7f6;
  --theme-bg-panel: rgba(255, 255, 255, 0.85);
  --theme-bg-card: hsla(0, 0%, 100%, 0.6);
  --theme-bg-overlay: rgba(255, 255, 255, 0.6);
  --theme-bg-glass: rgba(255, 255, 255, 0.7);
  --theme-bg-input: #ffffff;

  /* 柔和的边框 */
  --theme-border: rgba(82, 190, 128, 0.2);
  --theme-border-hover: hsla(160, 35%, 45%, 0.4);

  /* 深色文字以保证对比度 */
  --theme-text-main: #2c3e50;
  --theme-text-secondary: #5d6d7e;
  --theme-text-dim: #85929e;

  /* 柔和的阴影 */
  --theme-shadow: hsla(160, 35%, 45%, 0.15);
  --theme-shadow-strong: hsla(160, 35%, 45%, 0.25);
  --theme-shadow-flat: 0 2px 10px rgba(0, 0, 0, 0.05);
  --theme-shadow-raised: 0 4px 20px rgba(0, 0, 0, 0.1);

  /* 覆盖特定颜色 */
  --text-hextech: hsl(200, 60%, 45%);
  --rich-text-highlight: hsl(160, 45%, 40%);
  --rich-text-emphasis: hsl(15, 70%, 55%);
  --rich-text-special: hsl(260, 40%, 55%);
  --rich-text-quote: hsl(190, 50%, 45%);
}

/* ============================================
   Dark 主题（默认，深色主题）
   ============================================ */
[data-theme="dark"] {
  /* 默认主题已经在 :root 中定义，这里可以覆盖特定元素 */
}
```

**主题切换实现（TypeScript/React）：**

```typescript
// 切换主题
function setTheme(theme: 'dark' | 'relaxed' | 'custom'): void {
  document.documentElement.setAttribute('data-theme', theme);
  localStorage.setItem('preferred-theme', theme);
}

// 初始化主题
function initializeTheme(): void {
  const savedTheme = localStorage.getItem('preferred-theme') || 'dark';
  setTheme(savedTheme as 'dark' | 'relaxed' | 'custom');
}

// 在应用初始化时调用
$(() => {
  initializeTheme();
});
```

**参考实现：**
- `src/adventure/components/GameScreen.css` - 主题系统实现
- `src/adventure/components/views/SettingsView.tsx` - 主题切换组件

#### 9.3 窗口大小和响应式设计

**基本规则（基于项目设计流程总结）：**

```css
/* ============================================
   窗口大小规范
   ============================================ */
#app {
  width: 100%;
  min-height: 100%;
  height: 100%;
  position: relative;
}

/* 全屏模式支持 */
html:fullscreen #app,
html:-webkit-full-screen #app,
html:-moz-full-screen #app,
html:-ms-fullscreen #app {
  width: 100vw !important;
  height: 100vh !important;
}
```

**响应式断点（基于 adventure 实际实现）：**

```css
/* ============================================
   响应式断点变量
   ============================================ */
:root {
  --breakpoint-xs: 320px;
  --breakpoint-sm: 480px;
  --breakpoint-md: 640px;
  --breakpoint-lg: 768px;
  --breakpoint-xl: 1024px;
  --breakpoint-2xl: 1200px;

  /* 容器内边距（响应式） */
  --container-padding-xs: 4px;
  --container-padding-sm: 8px;
  --container-padding-md: 12px;
  --container-padding-lg: 16px;
  --container-padding-xl: 20px;

  /* 文字大小（响应式） */
  --text-xs: 10px;
  --text-sm: 12px;
  --text-base: 14px;
  --text-lg: 16px;
  --text-xl: 18px;
  --text-2xl: 20px;
  --text-3xl: 24px;

  /* 间距（响应式） */
  --gap-xs: 4px;
  --gap-sm: 8px;
  --gap-md: 12px;
  --gap-lg: 16px;
  --gap-xl: 20px;
}
```

**移动端适配（基于 adventure 实际实现）：**

```css
/* ============================================
   移动端/小屏幕适配 (Responsive Design)
   ============================================ */
@media (max-width: 768px) {
  .game-screen {
    padding: 1rem;
  }

  .game-narrative-panel {
    height: clamp(300px, 50vh, 500px);
  }

  .game-narrative-content {
    padding: 1rem 1.5rem;
  }

  .game-narrative-text {
    font-size: 0.875rem;
    gap: 1rem;
  }

  /* 状态栏响应式 */
  .game-status {
    flex-wrap: wrap;
    gap: 0.75rem;
    padding: 0.75rem;
    background: rgba(0, 0, 0, 0.6);
    border: 1px solid rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
  }

  .game-status-item {
    flex-direction: column;
    align-items: flex-start;
    gap: 0.25rem;
    min-width: calc(50% - 0.375rem);
    padding: 0.5rem;
    background: rgba(255, 255, 255, 0.03);
    border-radius: 4px;
  }

  .game-status-label {
    font-size: 0.7rem;
    color: var(--theme-text-dim);
    opacity: 0.8;
  }

  .game-status-value {
    font-size: 0.85rem;
    color: var(--theme-text-main);
    font-weight: 700;
  }

  /* 选项按钮响应式 */
  .game-narrative-options {
    padding: 1rem;
    gap: 0.75rem;
  }

  .game-narrative-option {
    padding: 12px 16px;
    font-size: 14px;
    flex-wrap: wrap;
    min-height: auto;
    align-items: flex-start;
  }

  .game-narrative-option-id {
    min-width: auto;
    margin-right: 8px;
    flex-shrink: 0;
  }

  .game-narrative-option-text {
    flex: 1;
    min-width: 0;
    word-wrap: break-word;
    overflow-wrap: break-word;
    line-height: 1.4;
  }
}

/* 超小屏幕 (≤480px) */
@media (max-width: 480px) {
  .game-status-item {
    min-width: 100%;
  }

  .game-status {
    padding: 0.5rem;
    gap: 0.5rem;
  }

  .game-status-item {
    padding: 0.4rem;
  }

  .game-status-label {
    font-size: 0.65rem;
  }

  .game-status-value {
    font-size: 0.8rem;
  }

  .game-narrative-options {
    padding: 0.75rem;
  }

  .game-narrative-option {
    padding: 10px 12px;
    font-size: 13px;
  }
}
```

**关键要点：**
- ✅ 使用 `clamp()` 实现流体尺寸（如 `clamp(300px, 50vh, 500px)`）
- ✅ 使用 `calc()` 计算响应式间距（如 `calc(50% - 0.375rem)`）
- ✅ 使用 `flex-wrap: wrap` 允许元素换行
- ✅ 使用 `min-width: 0` 防止 flex 子元素溢出
- ✅ 使用 `word-wrap: break-word` 处理长文本

#### 9.4 玻璃态效果（Glassmorphism）

**基于 adventure 实际实现的玻璃态效果：**

```css
/* ============================================
   玻璃态效果 (Glassmorphism)
   ============================================ */
.glass-panel {
  background: var(--theme-bg-glass);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid var(--theme-border);
  box-shadow: var(--theme-shadow-raised);
  border-radius: 12px;
}

.glass-card {
  background: var(--theme-bg-card);
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  border: 1px solid var(--theme-border);
  box-shadow: var(--theme-shadow-flat);
  border-radius: 8px;
}

.glass-overlay {
  background: var(--theme-bg-overlay);
  backdrop-filter: blur(30px);
  -webkit-backdrop-filter: blur(30px);
  border: 1px solid var(--theme-border);
}
```

**关键要点：**
- ✅ 使用 `backdrop-filter: blur()` 实现背景模糊
- ✅ 配合半透明背景色（`rgba` 或 `hsla`）
- ✅ 添加边框和阴影增强层次感
- ✅ 注意浏览器兼容性（`-webkit-backdrop-filter`）

#### 9.5 动画效果

**基于 adventure 实际实现的动画系统：**

```css
/* ============================================
   动画效果系统
   ============================================ */
/* 淡入淡出动画 */
@keyframes fadeIn {
  from {
    opacity: 0;
  }
  to {
    opacity: 1;
  }
}

@keyframes fadeOut {
  from {
    opacity: 1;
  }
  to {
    opacity: 0;
  }
}

/* 滑入滑出动画 */
@keyframes slideInUp {
  from {
    transform: translateY(100%);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

@keyframes slideInDown {
  from {
    transform: translateY(-100%);
    opacity: 0;
  }
  to {
    transform: translateY(0);
    opacity: 1;
  }
}

/* 缩放动画 */
@keyframes scaleIn {
  from {
    transform: scale(0.9);
    opacity: 0;
  }
  to {
    transform: scale(1);
    opacity: 1;
  }
}

/* 发光动画 */
@keyframes glow {
  0%, 100% {
    box-shadow: 0 0 20px var(--theme-shadow);
  }
  50% {
    box-shadow: 0 0 40px var(--theme-shadow-strong);
  }
}

/* 脉冲动画 */
@keyframes pulse {
  0%, 100% {
    opacity: 1;
  }
  50% {
    opacity: 0.5;
  }
}

/* 使用动画 */
.fade-in {
  animation: fadeIn var(--transition-normal) ease;
}

.slide-in-up {
  animation: slideInUp var(--transition-slow) cubic-bezier(0.4, 0, 0.2, 1);
}

.scale-in {
  animation: scaleIn var(--transition-normal) ease;
}

.glow-effect {
  animation: glow 2s ease-in-out infinite;
}

.pulse-effect {
  animation: pulse 2s ease-in-out infinite;
}
```

**过渡效果：**

```css
/* ============================================
   过渡效果
   ============================================ */
/* 通用过渡 */
.transition-all {
  transition: all var(--transition-normal) ease;
}

.transition-colors {
  transition: background-color var(--transition-fast) ease,
              color var(--transition-fast) ease,
              border-color var(--transition-fast) ease;
}

.transition-transform {
  transition: transform var(--transition-normal) cubic-bezier(0.4, 0, 0.2, 1);
}

/* 交互元素过渡 */
.interactive {
  transition: all var(--transition-fast) ease;
  cursor: pointer;
}

.interactive:hover {
  transform: translateY(-2px);
  box-shadow: var(--theme-shadow-raised);
}

.interactive:active {
  transform: translateY(0);
  box-shadow: var(--theme-shadow-flat);
}
```

**关键要点：**
- ✅ 使用 CSS 变量统一管理动画时长（`--transition-fast`、`--transition-normal` 等）
- ✅ 使用 `cubic-bezier` 缓动函数实现自然的动画效果
- ✅ 提供多种动画类型（淡入淡出、滑入滑出、缩放、发光、脉冲）
- ✅ 交互元素使用过渡效果提升用户体验

#### 9.6 字体规范

**基于 adventure 实际实现的字体系统：**

```css
/* ============================================
   字体规范
   ============================================ */
:root {
  /* 字体族 */
  --font-primary: 'Noto Serif SC', 'Microsoft YaHei', 'SimSun', serif;
  --font-secondary: 'Outfit', 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  --font-mono: 'Share Tech Mono', 'Courier New', monospace;

  /* 字体大小 */
  --font-size-xs: 0.75rem;    /* 12px */
  --font-size-sm: 0.875rem;   /* 14px */
  --font-size-base: 1rem;     /* 16px */
  --font-size-lg: 1.125rem;   /* 18px */
  --font-size-xl: 1.25rem;    /* 20px */
  --font-size-2xl: 1.5rem;    /* 24px */
  --font-size-3xl: 1.875rem;  /* 30px */
  --font-size-4xl: 2.25rem;   /* 36px */

  /* 字重 */
  --font-weight-normal: 400;
  --font-weight-medium: 500;
  --font-weight-semibold: 600;
  --font-weight-bold: 700;

  /* 行高 */
  --line-height-tight: 1.25;
  --line-height-normal: 1.5;
  --line-height-relaxed: 1.75;
}

/* 基础字体样式 */
body {
  font-family: var(--font-primary);
  font-size: var(--font-size-base);
  line-height: var(--line-height-normal);
  color: var(--theme-text-main);
  -webkit-font-smoothing: antialiased;
  text-rendering: optimizeLegibility;
}

/* 标题字体 */
h1, h2, h3, h4, h5, h6 {
  font-family: var(--font-primary);
  font-weight: var(--font-weight-bold);
  line-height: var(--line-height-tight);
  color: var(--theme-text-main);
}

h1 {
  font-size: var(--font-size-4xl);
}

h2 {
  font-size: var(--font-size-3xl);
}

h3 {
  font-size: var(--font-size-2xl);
}

/* 等宽字体（用于数据展示） */
.mono {
  font-family: var(--font-mono);
  font-size: var(--font-size-sm);
  letter-spacing: 0.05em;
}

/* 次要文字 */
.text-secondary {
  font-family: var(--font-secondary);
  color: var(--theme-text-secondary);
  font-size: var(--font-size-sm);
}
```

**关键要点：**
- ✅ 使用中文字体（如 Noto Serif SC、Microsoft YaHei）作为主字体
- ✅ 使用英文字体（如 Outfit、Inter）作为辅助字体
- ✅ 使用等宽字体（如 Share Tech Mono）展示数据和代码
- ✅ 使用 `-webkit-font-smoothing: antialiased` 提升字体渲染质量
- ✅ 提供多级字体大小和字重变量

#### 9.7 滚动条样式

**基于 adventure 实际实现的自定义滚动条：**

```css
/* ============================================
   自定义滚动条样式
   ============================================ */
/* Webkit 浏览器 (Chrome, Safari, Edge) */
::-webkit-scrollbar {
  width: 8px;
  height: 8px;
}

::-webkit-scrollbar-track {
  background: var(--theme-bg-main);
  border-radius: 4px;
}

::-webkit-scrollbar-thumb {
  background: var(--theme-border);
  border-radius: 4px;
  transition: background var(--transition-fast) ease;
}

::-webkit-scrollbar-thumb:hover {
  background: var(--theme-border-hover);
}

/* Firefox */
* {
  scrollbar-width: thin;
  scrollbar-color: var(--theme-border) var(--theme-bg-main);
}

/* 特定主题的滚动条 */
[data-theme="relaxed"] ::-webkit-scrollbar-thumb {
  background: var(--theme-scroll-thumb, rgba(82, 190, 128, 0.3));
}

[data-theme="relaxed"] * {
  scrollbar-color: var(--theme-scroll-thumb, rgba(82, 190, 128, 0.3)) var(--theme-bg-main);
}
```

#### 9.8 富文本样式

**基于 adventure 实际实现的富文本样式：**

```css
/* ============================================
   富文本样式（用于 maintext 显示）
   ============================================ */
.story-content {
  line-height: var(--line-height-relaxed);
  color: var(--theme-text-main);
  font-size: var(--font-size-base);
}

/* 富文本标签样式 */
.story-content .dialogue-text {
  color: var(--rich-text-highlight);
  font-weight: var(--font-weight-medium);
}

.story-content .emphasis-text {
  color: var(--rich-text-emphasis);
  font-style: italic;
}

.story-content .special-text {
  color: var(--rich-text-special);
}

.story-content .quote-text {
  color: var(--rich-text-quote);
  border-left: 3px solid var(--rich-text-quote);
  padding-left: 1rem;
  margin: 1rem 0;
  font-style: italic;
}

/* 富文本中的链接 */
.story-content a {
  color: var(--text-hextech);
  text-decoration: none;
  border-bottom: 1px solid var(--text-hextech);
  transition: all var(--transition-fast) ease;
}

.story-content a:hover {
  color: var(--theme-primary);
  border-bottom-color: var(--theme-primary);
}
```

**参考实现：**
- `src/adventure/components/GameScreen.css` - 富文本样式实现
- `src/adventure/utils/textParser.ts` - 富文本解析工具

#### 9.9 按钮和交互元素样式

**基于 adventure 实际实现的按钮样式：**

```css
/* ============================================
   按钮样式系统
   ============================================ */
.btn {
  padding: 0.75rem 1.5rem;
  border: 1px solid var(--theme-border);
  border-radius: 8px;
  background: var(--theme-bg-card);
  color: var(--theme-text-main);
  font-family: var(--font-secondary);
  font-size: var(--font-size-base);
  font-weight: var(--font-weight-medium);
  cursor: pointer;
  transition: all var(--transition-fast) ease;
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
}

.btn:hover {
  background: var(--theme-bg-panel);
  border-color: var(--theme-border-hover);
  transform: translateY(-2px);
  box-shadow: var(--theme-shadow-flat);
}

.btn:active {
  transform: translateY(0);
  box-shadow: none;
}

.btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  pointer-events: none;
}

/* 主按钮 */
.btn-primary {
  background: var(--theme-primary);
  color: #000;
  border-color: var(--theme-primary);
  font-weight: var(--font-weight-semibold);
}

.btn-primary:hover {
  background: var(--theme-primary-glow);
  box-shadow: 0 0 20px var(--theme-shadow);
}

/* 次要按钮 */
.btn-secondary {
  background: transparent;
  border-color: var(--theme-border);
}

.btn-secondary:hover {
  background: var(--theme-bg-glass);
  border-color: var(--theme-border-hover);
}

/* 选项按钮（游戏选项） */
.game-option-btn {
  width: 100%;
  padding: 1rem 1.5rem;
  text-align: left;
  background: var(--theme-bg-card);
  border: 1px solid var(--theme-border);
  border-radius: 12px;
  color: var(--theme-text-main);
  font-size: var(--font-size-base);
  line-height: var(--line-height-normal);
  transition: all var(--transition-normal) ease;
  backdrop-filter: blur(10px);
  -webkit-backdrop-filter: blur(10px);
  cursor: pointer;
}

.game-option-btn:hover {
  background: var(--theme-bg-panel);
  border-color: var(--theme-border-hover);
  transform: translateX(4px);
  box-shadow: var(--theme-shadow-flat);
}

.game-option-btn:active {
  transform: translateX(2px);
  box-shadow: none;
}

.game-option-btn:disabled {
  opacity: 0.5;
  cursor: not-allowed;
  pointer-events: none;
}
```

#### 9.10 模态框样式

**基于 adventure 实际实现的模态框样式：**

```css
/* ============================================
   模态框样式系统
   ============================================ */
.modal-overlay {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: var(--theme-bg-overlay);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  z-index: 1000;
  display: flex;
  align-items: center;
  justify-content: center;
  animation: fadeIn var(--transition-normal) ease;
}

.modal-container {
  background: var(--theme-bg-panel);
  border: 1px solid var(--theme-border);
  border-radius: 16px;
  box-shadow: var(--theme-shadow-raised);
  max-width: 90vw;
  max-height: 90vh;
  overflow: auto;
  animation: scaleIn var(--transition-normal) cubic-bezier(0.4, 0, 0.2, 1);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
}

.modal-header {
  padding: 1.5rem;
  border-bottom: 1px solid var(--theme-border);
  display: flex;
  align-items: center;
  justify-content: space-between;
}

.modal-title {
  font-size: var(--font-size-xl);
  font-weight: var(--font-weight-bold);
  color: var(--theme-text-main);
}

.modal-close-btn {
  background: transparent;
  border: none;
  color: var(--theme-text-dim);
  font-size: var(--font-size-2xl);
  cursor: pointer;
  width: 32px;
  height: 32px;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 4px;
  transition: all var(--transition-fast) ease;
}

.modal-close-btn:hover {
  background: var(--theme-bg-card);
  color: var(--theme-text-main);
}

.modal-body {
  padding: 1.5rem;
  color: var(--theme-text-main);
  line-height: var(--line-height-relaxed);
}

.modal-footer {
  padding: 1.5rem;
  border-top: 1px solid var(--theme-border);
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 1rem;
}
```

#### 9.11 状态栏和面板样式

**基于 adventure 实际实现的状态栏样式：**

```css
/* ============================================
   状态栏样式
   ============================================ */
.game-status {
  display: flex;
  align-items: center;
  gap: 1.5rem;
  padding: 1rem 1.5rem;
  background: var(--theme-bg-glass);
  backdrop-filter: blur(20px);
  -webkit-backdrop-filter: blur(20px);
  border: 1px solid var(--theme-border);
  border-radius: 12px;
  box-shadow: var(--theme-shadow-flat);
}

.game-status-item {
  display: flex;
  flex-direction: column;
  gap: 0.25rem;
}

.game-status-label {
  font-size: var(--font-size-xs);
  color: var(--theme-text-dim);
  font-weight: var(--font-weight-normal);
  text-transform: uppercase;
  letter-spacing: 0.05em;
}

.game-status-value {
  font-size: var(--font-size-lg);
  color: var(--theme-text-main);
  font-weight: var(--font-weight-bold);
  font-family: var(--font-mono);
}

.game-status-divider {
  width: 1px;
  height: 2rem;
  background: var(--theme-border);
  opacity: 0.5;
}
```

#### 9.12 全屏模式支持

**基于项目设计流程总结的全屏模式实现：**

```css
/* ============================================
   全屏模式支持
   ============================================ */
html:fullscreen #app,
html:-webkit-full-screen #app,
html:-moz-full-screen #app,
html:-ms-fullscreen #app {
  width: 100vw !important;
  height: 100vh !important;
  overflow: hidden;
}

html:fullscreen body,
html:-webkit-full-screen body,
html:-moz-full-screen body,
html:-ms-fullscreen body {
  overflow: hidden;
}
```

**全屏 API 实现（TypeScript）：**

```typescript
// 请求全屏
function requestFullscreen(): void {
  const doc = document.documentElement;
  if (doc.requestFullscreen) {
    doc.requestFullscreen().catch(err => console.log('全屏请求失败:', err));
  } else if ((doc as any).webkitRequestFullscreen) {
    (doc as any).webkitRequestFullscreen();
  } else if ((doc as any).mozRequestFullScreen) {
    (doc as any).mozRequestFullScreen();
  } else if ((doc as any).msRequestFullscreen) {
    (doc as any).msRequestFullscreen();
  }
}

// 退出全屏
function exitFullscreen(): void {
  if (document.exitFullscreen) {
    document.exitFullscreen();
  } else if ((document as any).webkitExitFullscreen) {
    (document as any).webkitExitFullscreen();
  } else if ((document as any).mozCancelFullScreen) {
    (document as any).mozCancelFullScreen();
  } else if ((document as any).msExitFullscreen) {
    (document as any).msExitFullscreen();
  }
}

// 监听全屏状态变化
document.addEventListener('fullscreenchange', () => {
  const isFullscreen = !!(
    document.fullscreenElement ||
    (document as any).webkitFullscreenElement ||
    (document as any).mozFullScreenElement ||
    (document as any).msFullscreenElement
  );
  console.log('全屏状态:', isFullscreen ? '已全屏' : '已退出全屏');
  // 更新 UI 状态
});
```

**参考实现：**
- `项目设计流程总结与提示词.md` - 全屏模式规范
- `src/adventure/components/GameScreen.tsx` - 全屏模式实现

#### 9.13 样式文件组织

**推荐的文件组织方式（基于 adventure 项目）：**

```
src/你的项目名/
├── components/
│   ├── GameScreen.tsx
│   ├── GameScreen.css          # 组件样式（如果使用 CSS）
│   └── GameScreen.module.css   # 组件样式（如果使用 CSS Modules）
├── styles/                      # 全局样式目录（可选）
│   ├── variables.scss          # SCSS 变量（如果使用 SCSS）
│   ├── main.scss               # 全局样式
│   └── responsive.scss         # 响应式样式
└── index.html                  # 主 HTML 文件
```

**样式导入方式：**

```typescript
// React 项目（CSS 文件）
import './GameScreen.css';

// React 项目（CSS Modules）
import styles from './GameScreen.module.css';

// React 项目（SCSS）
import './GameScreen.scss';

// 非 React 项目（在 index.html 中引入）
// <link rel="stylesheet" href="./styles/main.css">
```

**参考实现：**
- `src/adventure/components/GameScreen.css` - 组件样式实现
- `src/zhutiangame/styles/responsive.scss` - 响应式样式实现
- `项目设计流程总结与提示词.md` - 样式文件组织规范

#### 9.14 样式最佳实践总结

**核心原则：**
1. ✅ **使用 CSS 变量**：所有颜色、间距、字体大小都使用变量
2. ✅ **语义化命名**：使用语义化变量名（`--theme-primary` 而非 `--gold-color`）
3. ✅ **响应式优先**：使用 `clamp()`、`calc()`、`flex-wrap` 实现响应式
4. ✅ **性能优化**：避免过度使用 `backdrop-filter`，注意浏览器兼容性
5. ✅ **主题切换**：使用 `[data-theme]` 属性实现多主题支持
6. ✅ **动画统一**：使用统一的动画时长变量和缓动函数

**关键要点：**
- ✅ **CSS 变量 > SCSS 变量**：CSS 变量支持运行时切换，更适合主题系统
- ✅ **HSL > RGB/HEX**：HSL 格式便于调整明度和饱和度
- ✅ **移动端优先**：先设计移动端样式，再扩展到桌面端
- ✅ **玻璃态效果**：使用 `backdrop-filter` 实现现代 UI 效果
- ✅ **字体渲染**：使用 `-webkit-font-smoothing: antialiased` 提升字体质量

**参考实现：**
- `src/adventure/components/GameScreen.css` - 完整的样式系统实现
- `src/zhutiangame/styles/responsive.scss` - 响应式样式实现
- `项目设计流程总结与提示词.md` - 样式规范总结

## 📝 关键注意事项

### 1. MVU 变量框架的使用

- **读取变量**：优先从最新 assistant 消息读取，如果不存在则从最新楼层或0层读取
- **写入变量**：使用 `updateVariablesWith` 更新变量，使用 `Mvu.replaceMvuData` 写回解析后的变量
- **解析变量**：使用 `Mvu.parseMessage` 解析消息中的 `<UpdateVariable>` 标签

### 2. 消息标签规范

- **必须包含**：`<maintext>` 和 `<option>` 标签
- **可选包含**：`<sum>`、`<UpdateVariable>`、`<examine>`、`<look>` 等标签
- **必须移除**：`<thinking>` 标签（在流式生成和最终结果中都要移除）

### 3. 事件监听最佳实践

- **优先使用 `MESSAGE_UPDATED`**：这个事件在 `replaceMvuData` 完成后触发，数据更可靠
- **延迟刷新**：使用 `setTimeout` 延迟刷新，确保数据已完全写入
- **防止重复刷新**：使用标志位防止重复刷新

### 4. 错误处理

- **生成失败**：删除已创建的 user 消息，恢复界面状态
- **消息不规范**：验证消息格式，不符合规范则回退
- **变量读取失败**：使用默认值，记录警告日志

### 5. 样式规范最佳实践

- **使用 CSS 变量**：所有颜色、间距、字体大小都使用 CSS 变量，支持运行时切换
- **语义化命名**：使用 `--theme-primary` 而非 `--gold-color`，便于主题切换
- **HSL 颜色格式**：使用 HSL 而非 RGB/HEX，便于调整明度和饱和度
- **响应式优先**：使用 `clamp()`、`calc()`、`flex-wrap` 实现响应式布局
- **玻璃态效果**：使用 `backdrop-filter: blur()` 实现现代 UI 效果，注意浏览器兼容性
- **动画统一**：使用统一的动画时长变量（`--transition-fast`、`--transition-normal` 等）
- **移动端适配**：优先设计移动端样式（`max-width: 768px`），再扩展到桌面端
- **性能优化**：避免过度使用 `backdrop-filter`，注意 `will-change` 属性的使用

## 🔍 调试技巧

### 1. 检查变量读取

```typescript
// 在 variableReader.ts 中添加详细日志
console.log('📊 [variableReader] 尝试从最新 assistant 消息读取变量数据');
console.log('📊 [variableReader] 数据摘要:', {
  stat_data_keys: Object.keys(mvuData.stat_data || {}),
  has_display_data: !!mvuData.display_data,
});
```

### 2. 检查消息创建

```typescript
// 在 unifiedRequestHandler.ts 中添加验证
const verifyMessage = getChatMessages(assistantMessageId)?.[0];
if (verifyMessage?.data) {
  console.log('✅ 验证：消息 data 字段已存在');
  console.log('📊 消息 data 字段摘要:', {
    has_stat_data: !!verifyMessage.data.stat_data,
    stat_data_keys: Object.keys(verifyMessage.data.stat_data || {}),
  });
}
```

### 3. 检查事件触发

```typescript
// 在事件监听器中添加日志
eventOn(tavern_events.MESSAGE_UPDATED, (message_id: number) => {
  console.log('🔄 消息已更新，刷新游戏数据:', message_id);
  console.log('📊 当前时间:', new Date().toISOString());
});
```

## ✅ 检查清单

完成改造后，请确认以下功能是否正常工作：

- [ ] **开局流程**：楼层为0时显示开局表单，楼层>0时直接进入游戏
- [ ] **变量初始化**：开局表单提交后，变量正确写入0层
- [ ] **开局消息**：开局介绍楼层（1层）正确创建
- [ ] **变量读取**：游戏界面能正确读取最新楼层的变量
- [ ] **请求处理**：点击选项后能正确发送请求并生成回复
- [ ] **流式生成**：生成过程中能实时显示流式文本
- [ ] **事件监听**：消息更新后界面自动刷新
- [ ] **编年史更新**：编年史能正确更新
- [ ] **错误处理**：生成失败时能正确回退
- [ ] **样式系统**：CSS 变量系统正常工作，主题切换正常
- [ ] **响应式设计**：在不同屏幕尺寸下正常显示（桌面端、平板、手机）
- [ ] **玻璃态效果**：背景模糊效果正常（注意浏览器兼容性）
- [ ] **动画效果**：动画流畅，过渡自然
- [ ] **富文本样式**：富文本内容正确渲染，颜色和样式符合主题
- [ ] **按钮交互**：按钮 hover 和 active 状态正常，过渡效果流畅
- [ ] **模态框样式**：模态框正确显示，遮罩层和动画效果正常

## 📚 参考资源

### 项目实现参考

- **ADven（归档）**：`备份工作区/ADven/` — 游戏式面板、消息解析等
  - `备份工作区/ADven/utils/messageParser.ts` — 消息解析
  - `备份工作区/ADven/utils/mvuStatData.ts` — MVU 状态数据

- **Goth（归档）**：`备份工作区/Goth/` — 变量读取、请求处理、编年史等
  - `备份工作区/Goth/utils/variableReader.ts` — 变量读取
  - `备份工作区/Goth/utils/requestHandlerPhase3.ts` — 统一请求处理
  - `备份工作区/Goth/utils/chronicleUpdater.ts` — 编年史更新
  - `备份工作区/Goth/utils/messageParser.ts` — 消息解析

### 样式参考

- **样式库**：`样式库/found1/`、`样式库/案例/` — 可复制的 UI 片段
- **Goth 专属参考**：`备份工作区/Goth/参考/`

### 文档参考

- **MVU 变量框架文档**：`.cursor/rules/mvu变量框架.mdc` - MVU 变量框架使用指南
- **项目基本概念**：`.cursor/rules/项目基本概念.mdc` - 项目基本概念和最佳实践
- **项目设计流程总结**：`项目设计流程总结与提示词.md` - 页面流程设计、样式规范、组件化设计

### 样式技术参考

- **CSS 变量系统**：MDN CSS Custom Properties
- **玻璃态效果**：CSS `backdrop-filter` 属性
- **响应式设计**：CSS `clamp()`、`calc()`、`flex-wrap` 等函数
- **动画效果**：CSS `@keyframes`、`transition`、`animation` 属性

## 🎉 完成

完成以上步骤后，你的前端项目就具备开局流程、MVU、统一请求与编年史等核心功能。如果遇到问题，请对照 `备份工作区/ADven`、`备份工作区/Goth` 或查看调试日志。

## 💡 基于 Adventure 项目的提示词总结和最佳实践

### 核心提示词模板（用于 AI 辅助开发）

当你需要改造一个项目时，可以使用以下提示词：

#### 1. 游戏初始化提示词

```
我需要创建一个游戏初始化工具 (`utils/gameInitializer.ts`)，需要实现以下功能：

1. **initializeGameVariables** 函数：
   - 接收 `OpeningFormData` 类型的表单数据
   - 使用 `updateVariablesWith` 更新0层变量
   - 确保 `stat_data` 结构存在
   - 根据表单数据初始化游戏状态、公会信息等
   - 包含完整的错误处理和日志记录

2. **createOpeningStoryMessage** 函数：
   - 接收 `OpeningFormData` 类型的表单数据
   - 防止重复创建（检查1层消息是否存在，使用全局标志）
   - 构建包含 `<maintext>`、`<option>`、`<sum>` 标签的开局消息
   - 从0层读取 MVU 数据并携带到1层消息的 data 字段
   - 创建1层 assistant 消息，设置 `refresh: 'none'`
   - 使用重试机制更新编年史（延迟500ms，最多重试3次）
   - 包含完整的错误处理和日志记录

请参考 `src/adventure/utils/gameInitializer.ts` 的实现方式。
```

#### 2. 变量读取提示词

```
我需要创建一个变量读取工具 (`utils/variableReader.ts`)，需要实现以下功能：

1. **getGameMvuData** 函数（内部）：
   - 优先从最新 assistant 消息读取 MVU 数据（通过 `Mvu.getMvuData`）
   - 备用方案：从最新 assistant 消息的 `data` 字段读取
   - 退化方案：从最新楼层读取（`message_id: 'latest'`）
   - 最后方案：从0层读取（用于初始化数据）
   - 每个步骤都有详细的日志记录（使用 emoji 前缀，如 🔍、✅、⚠️）
   - 使用 `hasStatDataContent` 检查数据是否有效（不是空对象）

2. **readYourGameData** 函数（导出）：
   - 调用 `getGameMvuData` 获取数据
   - 使用 `pick` 函数从嵌套对象中提取值，支持 MVU 格式 `[值, "描述"]`
   - 处理数组格式的数据（如果值是 MVU 格式数组，取第一个元素）
   - 返回类型安全的数据结构（`YourGameData` 类型）
   - 包含详细的日志记录

3. **pick** 函数（内部工具函数）：
   - 支持路径字符串（如 `'游戏状态.资产'`）
   - 支持 MVU 格式 `[值, "描述"]`，自动提取实际值
   - 提供默认值作为回退

4. **ensureMvuInitialized** 函数（内部）：
   - 使用单例模式确保 MVU 只初始化一次
   - 使用 Promise 缓存避免重复初始化
   - 调用 `waitGlobalInitialized('Mvu')` 等待 MVU 初始化

请参考 `src/adventure/utils/variableReader.ts` 的实现方式，特别是数据读取的优先级和错误处理。
```

#### 3. 统一请求处理器提示词

```
我需要创建一个统一请求处理器 (`utils/requestHandler.ts`)，需要实现以下功能：

1. **handleUnifiedRequest** 函数：
   - 接收用户输入（选项文本或自定义输入）和回调函数
   - 支持流式和非流式生成模式（从 localStorage 读取设置）
   - 在提示词发送前根据当前楼层调整消息隐藏范围（楼层 >= 30 时隐藏 0 到 当前楼层-15）
   - 在提示词发送前管理预设提示词（开局阶段启用"开局COT"，正常阶段启用"COT"）
   - 获取基础 MVU 数据并传递到 user 消息
   - 创建 user 消息，设置 `refresh: 'none'`
   - 注册流式事件监听器（如果启用流式模式）
   - 调用 `generate` 生成 LLM 回复
   - 移除 `<thinking>` 和 `<think>` 标签
   - 验证消息格式（必须包含 `<maintext>` 标签）
   - 提取最后一次出现的标签（`<maintext>`、`<option>`、`<sum>`、`<UpdateVariable>`、`<mission>`）
   - 使用 `Mvu.parseMessage` 解析消息中的变量更新命令
   - 创建 assistant 消息，携带解析后的数据
   - 使用 `Mvu.replaceMvuData` 将更新后的变量写回楼层
   - 延迟更新编年史（延迟500ms）
   - 包含完整的错误处理（生成失败时删除 user 消息，恢复界面状态）

2. **标签提取函数**（导出）：
   - `extractLastMaintext`: 提取最后一次出现的 `<maintext>` 标签，忽略 `<thinking>` 标签内的内容
   - `extractLastOption`: 提取最后一次出现的 `<option>` 标签
   - `extractLastSum`: 提取最后一次出现的 `<sum>` 标签
   - `extractLastUpdateVariable`: 提取最后一次出现的 `<UpdateVariable>` 标签
   - `extractLastMission`: 提取最后一次出现的 `<mission>` 标签（如果使用）

3. **流式文本处理函数**（内部）：
   - `extractMaintextFromStream`: 从流式文本中提取 `<maintext>` 内容（从标签出现开始显示）
   - `extractMissionFromStream`: 从流式文本中提取 `<mission>` 内容
   - `removeThinkingTagsFromStream`: 移除流式文本中的 `<thinking>` 标签内容（包括未闭合的）

4. **消息隐藏管理函数**（可选，导出）：
   - `adjustMessageVisibility`: 根据当前楼层调整消息隐藏范围（用于读档时）

请参考 `src/adventure/utils/requestHandler.ts` 的实现方式，特别是流式生成、消息隐藏、预设提示词管理等高级功能。
```

#### 4. 编年史更新提示词

```
我需要创建一个编年史更新工具 (`utils/chronicleUpdater.ts`)，需要实现以下功能：

1. **checkAndUpdateChronicle** 函数（导出）：
   - 从最新楼层开始向下查找包含 `<sum>` 标签的消息
   - 使用 `findLatestSumMessage` 找到最新的包含 `<sum>` 的消息
   - 调用 `updateChronicleInWorldbook` 更新编年史
   - 包含完整的错误处理和日志记录

2. **findLatestSumMessage** 函数（内部）：
   - 从指定楼层开始向下查找（从高到低）
   - 使用 `parseSum` 解析消息中的 `<sum>` 标签
   - 返回找到的楼层号和 `<sum>` 内容，如果没找到返回 null

3. **parseSum** 函数（内部）：
   - 移除所有 `<thinking>` 和 `<think>` 标签及其内容
   - 检查是否有未闭合的标签，如果有则忽略
   - 提取最后一个 `<sum>` 标签的内容

4. **updateChronicleInWorldbook** 函数（内部）：
   - 计算条目编号：`Math.floor(楼层号 / 2) + 1`
   - 从世界书中获取编年史条目
   - 使用 `updateChronicleEntry` 更新或添加条目
   - 处理重roll（替换相同编号的条目）
   - 处理读档（清空编号 >= 当前条目编号的条目，但保留条目1）

5. **clearChronicleEntriesFromCurrentFloor** 函数（导出，可选）：
   - 清除编年史中等于或高于当前楼层对应条目数的信息
   - 用于避免历史编年史信息对当前重构情节造成锚定

请参考 `src/adventure/utils/chronicleUpdater.ts` 的实现方式，特别是条目编号计算和读档/重roll 处理逻辑。
```

#### 5. React 项目阶段管理提示词

```
我需要实现 React 项目的阶段管理，需要实现以下功能：

1. **App.tsx 组件**：
   - 定义游戏阶段枚举：`CLICK_TO_START` → `TITLE` → `LOADING` → `OPENING` → `GAME`
   - 在组件挂载时检查楼层数（`getLastMessageId()`）
   - 如果楼层为 0，显示开局表单；如果楼层 > 0，直接进入游戏
   - 处理开局表单提交：
     - 调用 `initializeGameVariables` 初始化变量（写入0层）
     - 调用 `createOpeningStoryMessage` 创建开局消息（1层）
     - 进入游戏阶段

2. **GameScreen.tsx 组件**：
   - 在组件挂载时加载游戏数据（`readYourGameData`）和消息内容（`loadFromLatestMessage`）
   - 监听以下事件：
     - `MESSAGE_UPDATED`: 消息更新时刷新数据（延迟300ms，防止重复刷新）
     - `MESSAGE_RECEIVED`: 收到新消息时刷新数据（延迟1000ms，等待 MESSAGE_UPDATED）
     - `CHAT_CHANGED`: 聊天文件变更时重新加载组件
   - 使用防抖机制避免重复刷新
   - 处理选项点击：调用 `handleUnifiedRequest`

3. **状态管理**：
   - 使用 React hooks（`useState`、`useEffect`、`useCallback`）
   - 管理游戏数据状态、消息内容状态、选项状态、生成状态等
   - 使用 `useRef` 保存待刷新定时器，避免重复刷新

请参考 `src/adventure/App.tsx` 和 `src/adventure/components/GameScreen.tsx` 的实现方式。
```

#### 6. 样式规范提示词

```
我需要实现项目的样式规范系统，需要实现以下功能：

1. **CSS 变量系统**：
   - 创建语义化变量系统（`--theme-primary`、`--theme-bg-main`、`--theme-text-main` 等）
   - 使用 HSL 颜色格式，便于调整明度和饱和度
   - 提供多层次的背景色（main、panel、card、overlay、glass）
   - 提供多层次的文字颜色（main、secondary、dim）
   - 统一的动画时长变量（`--transition-fast`、`--transition-normal`、`--transition-slow`）
   - 统一的阴影系统（`--theme-shadow`、`--theme-shadow-strong`、`--theme-shadow-flat`、`--theme-shadow-raised`）

2. **主题系统**：
   - 实现多主题支持（默认主题、relaxed 主题等）
   - 使用 `[data-theme="theme-name"]` 属性实现主题切换
   - 主题切换时更新所有 CSS 变量
   - 支持从 localStorage 读取和保存主题偏好

3. **响应式设计**：
   - 定义响应式断点变量（`--breakpoint-xs`、`--breakpoint-sm`、`--breakpoint-md` 等）
   - 实现移动端适配（`@media (max-width: 768px)`）
   - 实现超小屏幕适配（`@media (max-width: 480px)`）
   - 使用 `clamp()` 实现流体尺寸（如 `clamp(300px, 50vh, 500px)`）
   - 使用 `calc()` 计算响应式间距（如 `calc(50% - 0.375rem)`）
   - 使用 `flex-wrap: wrap` 允许元素换行
   - 使用 `min-width: 0` 防止 flex 子元素溢出

4. **玻璃态效果**：
   - 使用 `backdrop-filter: blur()` 实现背景模糊
   - 配合半透明背景色（`rgba` 或 `hsla`）
   - 添加边框和阴影增强层次感
   - 注意浏览器兼容性（`-webkit-backdrop-filter`）

5. **动画效果**：
   - 实现淡入淡出动画（`fadeIn`、`fadeOut`）
   - 实现滑入滑出动画（`slideInUp`、`slideInDown`）
   - 实现缩放动画（`scaleIn`）
   - 实现发光动画（`glow`）
   - 实现脉冲动画（`pulse`）
   - 使用 `cubic-bezier` 缓动函数实现自然的动画效果

6. **字体规范**：
   - 使用中文字体（如 Noto Serif SC、Microsoft YaHei）作为主字体
   - 使用英文字体（如 Outfit、Inter）作为辅助字体
   - 使用等宽字体（如 Share Tech Mono）展示数据和代码
   - 使用 `-webkit-font-smoothing: antialiased` 提升字体渲染质量
   - 提供多级字体大小和字重变量

7. **自定义滚动条**：
   - 使用 `::-webkit-scrollbar` 自定义 Webkit 浏览器滚动条
   - 使用 `scrollbar-width` 和 `scrollbar-color` 自定义 Firefox 滚动条
   - 滚动条颜色使用主题变量，支持主题切换

8. **富文本样式**：
   - 实现富文本标签样式（`dialogue-text`、`emphasis-text`、`special-text`、`quote-text`）
   - 使用主题变量定义富文本颜色
   - 支持富文本中的链接样式

9. **按钮和交互元素**：
   - 实现通用按钮样式（`.btn`）
   - 实现主按钮样式（`.btn-primary`）
   - 实现次要按钮样式（`.btn-secondary`）
   - 实现选项按钮样式（`.game-option-btn`）
   - 所有按钮都有 hover 和 active 状态
   - 使用过渡效果提升交互体验

10. **模态框样式**：
    - 实现模态框遮罩层（`.modal-overlay`）
    - 实现模态框容器（`.modal-container`）
    - 实现模态框头部、主体、底部样式
    - 使用动画效果（`fadeIn`、`scaleIn`）提升用户体验

11. **状态栏样式**：
    - 实现状态栏容器样式（`.game-status`）
    - 实现状态项样式（`.game-status-item`）
    - 实现状态标签和值样式
    - 支持响应式布局（移动端自动换行）

12. **全屏模式支持**：
    - 使用 `html:fullscreen` 选择器实现全屏模式样式
    - 实现全屏 API 函数（`requestFullscreen`、`exitFullscreen`）
    - 监听全屏状态变化事件

请参考 `src/adventure/components/GameScreen.css` 的实现方式，特别是 CSS 变量系统、主题系统、响应式设计和玻璃态效果。
```

### 关键实践要点总结

#### 1. 样式规范实践

**CSS 变量系统：**
```
✅ 使用语义化变量名（--theme-primary 而非 --gold-color）
✅ 使用 HSL 颜色格式，便于调整明度和饱和度
✅ 提供多层次的背景色和文字颜色
✅ 统一的动画时长和阴影系统
```

**主题系统：**
```
✅ 使用 [data-theme] 属性实现多主题支持
✅ 主题切换时更新所有 CSS 变量
✅ 支持从 localStorage 读取和保存主题偏好
```

**响应式设计：**
```
✅ 移动端优先：先设计移动端样式，再扩展到桌面端
✅ 使用 clamp()、calc()、flex-wrap 实现响应式布局
✅ 使用 min-width: 0 防止 flex 子元素溢出
✅ 使用 word-wrap: break-word 处理长文本
```

**玻璃态效果：**
```
✅ 使用 backdrop-filter: blur() 实现背景模糊
✅ 配合半透明背景色（rgba 或 hsla）
✅ 注意浏览器兼容性（-webkit-backdrop-filter）
```

**动画效果：**
```
✅ 使用 CSS 变量统一管理动画时长
✅ 使用 cubic-bezier 缓动函数实现自然的动画效果
✅ 提供多种动画类型（淡入淡出、滑入滑出、缩放、发光、脉冲）
```

#### 2. 数据流向

```
开局流程：
用户提交表单 → initializeGameVariables (写入0层) → createOpeningStoryMessage (创建1层，携带0层data)

游戏流程：
用户点击选项 → handleUnifiedRequest → 创建 user 消息（携带最新 data） → generate → 创建 assistant 消息（携带解析后的 data） → replaceMvuData (写回变量) → 界面刷新

数据读取：
界面刷新 → readYourGameData → getGameMvuData (从最新 assistant 消息读取) → 使用 pick 函数提取数据
```

#### 2. 错误处理最佳实践

- ✅ **每个关键步骤都有 try-catch**
- ✅ **使用详细的日志记录**（带 emoji 前缀，如 🔍、✅、⚠️、❌）
- ✅ **关键操作使用重试机制**（如编年史更新）
- ✅ **生成失败时回退**（删除 user 消息，恢复界面状态）
- ✅ **使用默认值作为回退**（如数据读取失败时返回空对象）

#### 3. 性能优化最佳实践

- ✅ **使用防抖机制**避免重复刷新（延迟300-1000ms）
- ✅ **使用全局标志**防止重复操作（如 `isCreatingOpening`）
- ✅ **使用单例模式**确保 MVU 只初始化一次
- ✅ **延迟更新非关键操作**（如编年史更新，延迟500ms）
- ✅ **使用 `refresh: 'none'`** 避免不必要的界面刷新

#### 4. 代码组织最佳实践

- ✅ **工具函数按功能分类**（`gameInitializer.ts`、`variableReader.ts`、`requestHandler.ts` 等）
- ✅ **使用 TypeScript 类型定义**（在 `types.ts` 中定义所有类型）
- ✅ **使用导出接口**提供类型安全的数据访问
- ✅ **内部函数使用详细注释**说明功能和注意事项
- ✅ **参考实际项目代码**（adventure、horr、mhjg）

### 常见问题和解决方案

#### Q1: 变量读取失败，返回空对象

**原因**：
- MVU 未初始化
- 消息还未完全写入
- 数据路径不正确

**解决方案**：
- 使用 `ensureMvuInitialized` 确保 MVU 已初始化
- 使用延迟读取（延迟300-500ms）
- 检查数据路径是否正确（使用 `pick` 函数）
- 添加详细的日志记录定位问题

#### Q2: 生成失败，user 消息已创建但 assistant 消息未创建

**原因**：
- 生成超时
- 消息格式验证失败
- 网络问题

**解决方案**：
- 在 `handleUnifiedRequest` 中捕获错误，删除已创建的 user 消息
- 恢复界面状态（隐藏生成提示框，重新启用选项按钮）
- 显示错误信息给用户

#### Q3: 编年史更新失败

**原因**：
- 消息还未完全写入
- 世界书条目不存在
- 条目编号计算错误

**解决方案**：
- 使用重试机制（延迟500ms，最多重试3次）
- 检查世界书名称和条目名称是否正确
- 检查条目编号计算公式（`Math.floor(楼层号 / 2) + 1`）

#### Q4: 流式生成时显示的内容不完整

**原因**：
- 流式事件监听器未正确注册
- `<thinking>` 标签未正确移除
- `<maintext>` 标签未正确提取

**解决方案**：
- 确保在调用 `generate` 之前注册流式事件监听器
- 使用 `extractMaintextFromStream` 正确提取流式文本中的 `<maintext>` 内容
- 确保在流式事件处理完成后移除事件监听器

### 参考资源

- **ADven（归档）**：`备份工作区/ADven/`
- **Goth（归档）**：`备份工作区/Goth/`
- **MVU 变量框架文档**：`.cursor/rules/mvu变量框架.mdc`
- **项目基本概念**：`.cursor/rules/项目基本概念.mdc`

---

**完成提示**：完成以上所有步骤后，你的项目就具备了完整的游戏初始化、变量管理、请求处理、编年史更新等核心功能。如果遇到问题，请对照 `备份工作区/ADven`、`备份工作区/Goth` 或查看详细的调试日志。